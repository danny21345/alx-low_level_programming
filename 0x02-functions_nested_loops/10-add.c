#include "main.h"

/**
* add - adds 2 nums and return sum
* @a: first argument
* @b: second argument
*
* simple arithmetic is applied
*
* Return: returns the sum
*/

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
