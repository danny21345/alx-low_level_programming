#include <stdio.h>

/**
* main - Uses the printf function to print a text
*
* This program use main to print given followed by new line
*
* Return: This program returns 0
*
*/

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
